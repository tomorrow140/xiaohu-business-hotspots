# PROGRESS

## 当前任务目标
建立轻量的小胡热点反馈闭环：小胡提交“反馈需求、需求类型”，产品经理在飞书文档中确认“是否要解决、解决状态、解决时间”。

## 已完成事项
- 已移除采集脚本里的 `MIN_MEDIA_CHANNELS = 2` 硬门槛。
- 已将 GitHub Actions 质量门槛从至少 1 条恢复为至少 3 条。
- 已恢复 2026-07-23 的页面数据候选池，并过滤 `AI早报`、`8点1氪`、观点句标题。
- 当前页面数据恢复为 13 条热点。
- 已为两条采集路径加入正文观点句标题过滤规则。
- 已通过 GitHub API 发布到线上页面，并确认 GitHub Pages 状态为 `built`。
- 已在每条热点标题旁增加轻量“反馈”按钮。
- 已在页头工具区增加弱化展示的“整体反馈”入口。
- 两类入口共用一个反馈弹窗；单条反馈会自动携带热点编号和标题。
- 反馈数据结构已包含反馈范围、类型、评论、提交人、页面地址、数据采集时间和待评审状态。
- 接收地址未配置时，反馈会暂存到当前浏览器并尝试复制，页面不会误报已送达。
- 已验证桌面和 390px 手机宽度，页面无横向溢出且弹窗交互正常。
- 已用飞书用户身份创建多维表格“小胡热点反馈池”，初始数据表为“反馈记录”。
- 已创建“小胡热点反馈”表单，并将反馈类型、反馈范围和具体反馈设为必填。
- 已将产品状态、产品经理意见和目标版本保留为后台审核字段，不展示在提交表单中。
- 已创建“待评审”和“已确认”两个视图，并验证筛选条件。
- 已停用多维表格作为主要反馈台账，原有 Base 保留且暂不继续接入。
- 已通过飞书 CLI 创建文档“小胡热点反馈与迭代记录”。
- 飞书文档地址：`https://my.feishu.cn/docx/VFa7djcg3oLtO2xTbS7c1nUbn5l`。
- 飞书文档已固定五个字段：反馈需求、需求类型、是否要解决、解决状态、解决时间。
- 已在飞书文档中登记首条真实需求“简化反馈流程”，状态为已解决，解决时间为 2026-07-23。
- 已将飞书文档改为原生五列表格，两条历史反馈均已写入表格。
- 页面反馈弹窗已移除称呼和复杂判断选项，只保留需求类型与反馈需求。
- 页面提交内容会自动补齐“待确认、待评估、待定”，复制标准记录并打开飞书文档。
- 页面复制内容已改为 Tab 分隔的一行五列，可直接粘贴进飞书文档表格。
- 已通过 GitHub 应用把反馈简化页面发布到 `main`，远端文件回读验证通过。

## 正在处理的文件
- `insights/weekly-hotspots.html`
- `PROGRESS.md`
- `TODO.md`

## 已做出的关键决策
- 单信源报道不再作为硬性排除条件。
- 多信源报道应作为置信度和排序加分信号，后续再完善。
- 早报、合集、正文观点句继续过滤，避免把多个主题混成一条热点。
- 全局反馈入口放在页头工具区，以弱化按钮呈现。
- 单条与全局反馈共用数据结构，通过 `scope` 字段区分。
- 飞书接收地址通过 `window.XIAOHU_FEEDBACK_ENDPOINT` 配置，避免重做页面交互。
- 飞书表单只承载小胡填写字段，产品经理审核字段留在数据表后台。
- “待评审”视图同时接收产品状态为空和明确标记为待评审的记录。
- 飞书 `form-questions-delete` 会连带删除底层字段；已恢复误删字段，后续不再用该命令隐藏审核字段。
- 页面只要求小胡填写反馈需求和需求类型，产品经理负责维护其余三个字段。
- 飞书文档为主要可读台账；原有多维表格只保留历史配置，不再作为当前流程入口。
- 飞书文档使用原生表格承载反馈，每条需求占一行。
- GitHub Pages 静态页面无法直接调用本机飞书 CLI；当前使用“复制记录并打开文档”，后续安全接口负责自动追加。
- 本机 `git push` 因无法连接 GitHub 443 端口失败，本轮改用 GitHub Contents API 发布；网络恢复后需重新同步本地与远端提交历史。

## 尚未完成事项
- 后续需要重构聚类逻辑，让同一主题自动汇总多家媒体报道。
- 后续需要把多信源报道作为排序和置信度加分信号。
- 待确认小胡是否拥有飞书文档编辑权限。
- 待部署安全的提交接口，把页面反馈自动追加到飞书文档。
- 待将产品经理确认后的记录自动同步成 GitHub 迭代待办。
- 待在 GitHub HTTPS 连接恢复后同步本地 `main` 与远端 `main`。

## 下一步最小可执行动作
在本地页面提交一条测试反馈，把复制内容粘贴到飞书表格的首个空白单元格，确认五列能正确展开。

## 当前是否有未提交改动
无。本轮“飞书文档原生表格”改动保存在提交 `Use embedded Feishu feedback table` 中。

## 如何验证当前结果
- `python3 -m py_compile automation/collect_free_news.py automation/collect_business_hotspots.py`
- 检查 `insights/hotspots-data.js` 中热点数量为 13。
- 检查页面中无 `AI早报`、`8点1氪`、`36氪早报` 等合集标题。
- `node` 解析页面内脚本，无语法错误。
- 浏览器验证“整体反馈”和单条“反馈”弹窗可以打开，且单条反馈自动显示对应资讯标题。
- 390px 手机宽度下 `body.scrollWidth` 与视口宽度一致，控制台无错误。
- `node` 解析页面内脚本，无语法错误。
- 飞书文档回读结果包含五个标准字段和首条真实需求。
- 飞书文档回读结果包含一个五列表格和两条反馈记录。
- 本地页面反馈弹窗只显示“需求类型”和“反馈需求”。
- 提交按钮文案为“登记到飞书”，复制内容为 Tab 分隔的五列表格行。
